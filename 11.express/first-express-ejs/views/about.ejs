<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About Page</title>
  </head>
  <body>
    <main>
      <h1>About me</h1>
      <h2><%= user.firstName %> <%= user.lastName %></h2>
      <p>My note is: <%= user.note %></p>
      <p>
        	<% if(user.note>10){%>
<!--  -->
                            Congratulations
                            <!--  -->
                            				<% }else{ %>
Rot treviser
				<% }%>

      </p>
      <ul>
        <% user.skills.forEach((skill)=>{ %>
             <%- include("components/card", {skill}) %>
		<% }) %>

      </ul>
    </main>
  </body>
</html>
